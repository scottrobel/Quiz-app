<h3 class="top-message"> <%= @quiz.title %> Responses </h3>
<% @quiz.responses.includes(:user).each do |response| %>
  <div class="top-message"><%= link_to "#{response.user.user_name}", response_path(response.id)%></div>
<% end %>