$('#page-wraper').html("<%= j render 'quizzes/form' %>");
$('.auto-update').change(updateForm);
function updateForm(){
  $("#update_list").click();
}
$('input[type="checkbox"]').change(updateForm);
function updateForm(){
  $("#update_list").click();
}